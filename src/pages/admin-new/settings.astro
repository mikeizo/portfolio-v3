---
import { fetchData } from '@/utils/mongodb'

import AdminNew from '@/layouts/AdminNew.astro'
import SettingsPage from '@/components/admin-new/pages/Settings.vue'

const [settingsData] = (await fetchData('settings')) ?? []
---

<AdminNew>
  <SettingsPage data={settingsData} client:load />
</AdminNew>
