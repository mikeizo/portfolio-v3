---
import { fetchData } from '@/utils/mongodb'

import AdminWorkList from '@/components/admin-new/pages/AdminWorkList.vue'
import AdminNew from '@/layouts/AdminNew.astro'

const work = (await fetchData('work', { sort: 'weight', order: 1 })) ?? []
---

<AdminNew>
  <AdminWorkList data={work} client:load />
</AdminNew>
