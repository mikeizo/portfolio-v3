---
import AdminNew from '@/layouts/AdminNew.astro'
import LoginPage from '@/components/pages/Login.vue'

if (Astro.locals.user) {
  return Astro.redirect('/admin')
}
---

<AdminNew isLogin={true}>
  <LoginPage client:load />
</AdminNew>
